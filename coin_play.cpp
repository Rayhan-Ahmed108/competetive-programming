#include <bits/stdc++.h>
using namespace std;
int main()
{

    int t;
    cin >> t;
    while (t--)
    {

        int a, b, c, d;
        cin >> a >> b >> c >> d;

        if (d / c <= a && d % c <= b)
        {

            cout << "YES" << '\n';
        }
        else if(d>=)
        else
            cout << "NO" << '\n';
    }
}